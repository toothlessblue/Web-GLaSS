#include <algorithm>
#include <vector>
#include "../components/Transform/Transform.hpp"

namespace Utils {
    namespace Vector
    {
        template <typename T> T remove(std::vector<T> vector, T value);
    }
}